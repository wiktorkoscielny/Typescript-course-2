import styled from 'styled-components'

//styling an aside elements (its kinda aside element)
export const Wrapper = styled.aside`
    font-family: Arial, Helvetica, sans-serif;
    width: 500px;
    padding: 20px;
`;